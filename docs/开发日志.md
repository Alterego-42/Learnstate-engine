# 开发日志

## 2026-04-05

### Clarity Review 复盘页热修
- 分支：`codex/review-clarity-p0`
- 复盘页已按“先结论、后证据、再建议”重排，顶部先回答“整体怎样 / 中间哪里卡住 / 最后怎么结束”。
- 已吸收 `Process Narrator` 的最小有效内容：`REPORT_GENERATION_STEPS` 与 `reportProcessCaption`，用于解释“为什么生成这么快但不是随便生成”。
- 已把 `steady_flow`、`looping`、`friction`、`backtrack_loop_score` 等技术表达翻成学习语言，并对乱码证据做前端过滤。
- 已补空数据 / 少样本兜底：无明显转折、无明显卡点、样本较少时都给可读提示，不再只显示空白列表。

### 当前状态
- 比赛版 MVP 已完成 demo-ready 基线冻结准备。
- 已完成闭环彩排一次，成功 `session_id`：`79817554-4ec0-47eb-8905-da81ee44e4ee`
- 本轮状态轨迹：`steady_flow -> looping -> steady_flow`
- 彩排结果：`event_count=55`、`snapshot_count=12`、`state_count=12`、`report_ready=true`

### 当前可演示链路
- 执行页：`http://127.0.0.1:5173/`
- daemon/API：`http://127.0.0.1:8765/`
- fallback history：
  - `http://127.0.0.1:8765/api/state/history?user_local_id=local-demo-user&session_id=79817554-4ec0-47eb-8905-da81ee44e4ee`
- fallback report：
  - `http://127.0.0.1:8765/api/session/79817554-4ec0-47eb-8905-da81ee44e4ee/report`

### Git 管理策略
- 先冻结当前 demo-ready 基线。
- 后续修改按分支推进。
- 每批改动必须验证后提交。
- 文档纳入版本管理：
  - `PRD.md`
  - `demo-checklist.md`
  - `阶段Agent回复/`
  - `docs/问题清单.md`
  - `docs/开发日志.md`

### 备注
- 当前处于“体验反馈收口阶段”。
- 默认不打断现有收口开发，除非问题被判定为 `P0` 并进入热修流程。

### Signal Forge 参考区信号兼容热修
- 分支：`codex/signal-forge-reference-events-p0`
- 本轮支持 `reference.open`、`reference.close`、`reference.section_change`、`reference.scroll` 写入 `raw_events`
- `FeatureSnapshot` 新增侧记字段 `reference_event_count`，但 `reference.*` 默认不计入 `event_count / edit_count / run_count`
- 若窗口内只有 `reference.*` 事件，则跳过本轮 `StateVector` 写回，避免空特征推理影响 `current/history/report`
- 已补充 `daemon/API_CONTRACT.md`、`daemon/README.md` 和 `docs/热修记录-reference-signal.md`
- `Process Narrator` 不再单独成线；其最小流程说明已并入 `codex/review-clarity-p0`

### Northstar Workbench 执行页参考区热修
- 分支：`codex/northstar-reference-zone-p0`
- 提交：`35adc28`
- 在执行页任务卡内新增可收起的“学习参考区”，默认与编码区共存，不改变主工作区结构。
- 参考区当前按任务提供 `题目目标`、`思路框架`、`学习步骤` 三个最小可用分区，并绑定 `demo-two-sum`、`demo-binary-search` 本地静态资料。
- 前端接入 `reference.open`、`reference.section_change`、`reference.scroll`、`reference.close` 轻量事件，不改状态机主链路。
- 已补充 `docs/热修记录-reference-zone.md`，并在 `frontend` 下执行 `npm run build` 验证通过。

### P0 热修验收与阶段挂起
- `Gatekeeper QA` 已在合并态完成 P0 验收，结论为“可带保留项收口”。
- 本轮已合并的 P0 热修包含：
  - 执行页学习参考区
  - `reference.*` 兼容采集
  - 复盘页可读性改造
  - 复盘生成流程说明
- 但从最终体验判断，本轮 P0 对用户体验的改善仍未达到预期目标：
  - 学习活动感仍然不足
  - 参考对象与反馈吸收链路仍偏弱
  - 因此相关问题先挂起，不继续扩展热修
- 决策：
  - 暂停当前 MVP 开发
  - 保留现有 demo-ready + P0 热修结果
  - 后续如重启，将以新的学习活动设计视角重新评估执行页和复盘页
